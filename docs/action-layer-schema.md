# Action-layer JSON schema

> **For the current, always-accurate list of tools and parameters, run
> `actiond --list-tools --format=human` or `actiond --list-tools --format=ai`** (see
> [Live tool catalog](#live-tool-catalog) below). Both are generated
> directly from `ActionRegistry`'s own schema data (`src/libs/actionlayer/action_registry.cpp`'s
> `registerBuiltinActions()`, which pairs every handler with its `ActionSchema` at the same
> registration call — see `src/libs/actionlayer/action_schema.h`), so they cannot drift from what
> is actually registered the way this hand-maintained document can. This document is still useful
> as a browsable, committed, prose-narrative reference — but if anything below looks stale,
> regenerate/cross-check it against the live command rather than trusting this file blindly.

This documents every action op the action layer (`src/libs/actionlayer/`) supports, as run by
`seamly2d-actiond` (`src/app/actiond/`) — either as one batch via the one-shot `--actions <file>`
CLI mode, or as a line in the persistent NDJSON daemon's request stream (see
[Session protocol](#session-protocol) below). This file is a **refresh grounded directly in the
handler source** (20 Aug 2026): every field name, type, default, literal-vs-formula distinction,
`Create()` mapping, and response/error shape below was read from `src/libs/actionlayer/handlers/*`
and cross-checked against the exact `VToolXxx::Create()` overload each handler calls — not
transcribed from an earlier draft of this file or from the design plan. See
[Findings](#findings) at the end for what changed/was added during this refresh, and
[How this file is maintained](#how-this-file-is-maintained) for the template to use when a new op
is added.

## How this file is maintained

This file is **generated by hand** from `src/libs/actionlayer/action_registry.cpp`'s registration
list and each handler's own source — those remain the source of truth; re-check them if this file
and the code ever disagree, and prefer re-reading the handler over trusting an old copy of this
doc. When you add a new op, append one entry in the matching category section below using this
template (copy it, replace angle-bracket placeholders, delete this comment line):

```
### `<op name>`

**Maps to:** `ClassName::Create(...)` — `path/to/file.cpp:LINE` (declaration `path/to/file.h:LINE`)

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | ... |

**Example request:**
```json
{ "op": "...", ... }
```

**Example success response:**
```json
{ ... }
```

**Known error cases:** ...
```

Update the [coverage summary](#coverage-summary) table in the same change.

## Live tool catalog

`actiond` has a third mode, alongside one-shot and daemon mode: `actiond --list-tools --format=<human|ai>`.
It needs no `--pattern`/`--measurements`/`--actions` at all — it is pure introspection over
`ActionRegistry`'s own schema data and exits immediately after printing.

- `actiond --list-tools --format=human` — grouped by category (the same nine sections this
  document uses: introspection, point, formula-point, curve, cut-point, operation, piece,
  measurements, session), each op showing its description, a parameter table (name, type,
  required/optional-with-default, and a literal-vs-formula note baked into each parameter's own
  description text), and one worked example request.
- `actiond --list-tools --format=ai` — a single JSON array on stdout (nothing else mixed in),
  shaped like a standard LLM tool-definition list: `[{"name", "description", "category",
  "input_schema": {"type": "object", "properties": {...}, "required": [...]}}, ...]`. An op with a
  documented behavioral gap (currently `piece.union`, `piece.insertNodes` — see their own entries
  below) additionally carries `"status": "partial"` and `"statusReason"`, rather than being
  silently omitted.

Both are generated from the exact same `ActionSchema` data every op is registered with in
`action_registry.cpp`'s `registerBuiltinActions()` (see `src/libs/actionlayer/action_schema.h`,
`src/libs/actionlayer/tool_catalog.h`/`.cpp`) — there is no separate, hand-maintained copy of this
data the way `pattern.listTools`'s own JSON action (a read-only *in-script* op, distinct from this
CLI flag) still is. `src/test/ActionLayerTest/tst_action_schema.cpp` and
`tests/actionlayer/run_batch`'s own `checkListToolsAi()` check both regression-test this against
future drift: the former asserts every registered handler has exactly one schema (in-process,
against `ActionRegistry` directly), the latter asserts the real `actiond --list-tools --format=ai`
subprocess output is valid, non-empty JSON.

## Session protocol

`seamly2d-actiond` has two modes:

- **One-shot** (`--actions <file>` given): load a pattern, run that one script, optionally save
  (`--save-pattern <path>`), print the JSON result, exit.
- **Persistent NDJSON daemon** (`--actions` omitted): `--pattern` and `--measurements` become
  optional (omit `--pattern` to start from an empty pattern), `--output-dir` (default `./output`)
  is where relative `render.snapshot`/`session.save` paths land, and the process reads one JSON
  request per stdin line, dispatches its `"actions"` array, and writes one JSON response per
  stdout line, until stdin reaches EOF or a `session.close` action is processed.

Request line:

```json
{"id": "batch-1", "onError": "abort", "actions": [{"op": "basePoint", "name": "A", "x": 0, "y": 0}]}
```

- `id` (optional, any JSON value): echoed back verbatim in the response, `null` if omitted.
- `onError` (optional, default `"abort"`): `"abort"` stops dispatch at the first failing action
  (`"results"` covers only what actually ran); `"continue"` runs every action regardless.
- `actions` (required, array): each entry is one action object, `{"op": "...", ...}`.

Response line (always one line, compact JSON, flushed immediately):

```json
{"id": "batch-1", "status": "ok", "appliedCount": 1,
 "results": [{"op": "basePoint", "index": 0, "status": "ok", "result": {"id": 1, "name": "A"}, "error": null}],
 "error": null}
```

- `status`: `"ok"` (every action in the request ran and succeeded), `"partial"` (`onError:
  "continue"` and at least one action failed), or `"error"` (aborted early, or the request itself
  was malformed — see below).
- `appliedCount`: number of actions that succeeded.
- `results`: one entry per action actually dispatched, each `{"op","index","status":"ok"|"error",
  "result": <payload or null>, "error": <null or message/structured object>}`.
- `error`: top-level failure reason for something *outside* action dispatch — a JSON parse
  failure, or a request missing/mistyping `"actions"`. `null` on every other path (per-action
  failures live inside `results`, not here).

`session.close` (see below) ends the loop after its batch's response is written.

**One-shot mode's response shape is different from the daemon shape documented above.** One-shot
mode (`--actions <file>`, no daemon) prints `ActionEngine::run()`'s own raw output directly, with
no `id`/`status`/`appliedCount` wrapper and no `index` per result:

```json
{"results": [{"op": "basePoint", "ok": true, "value": {"id": 1, "name": "A"}, "error": ""}]}
```

Only the persistent daemon (`SessionServer::processLine()`/`remapResult()`,
`src/app/actiond/session_server.cpp`) remaps this into the `{id,status,appliedCount,results:[
{op,index,status,result,error}],error}` shape shown above. A caller driving one-shot mode should
read `result.ok` (bool) and `result.error`, not `result.status`; a caller driving the daemon should
read `result.status` (`"ok"|"error"`) and `result.result`, not `result.value`. See
`tests/actionlayer/expected/*.expected.json` for real one-shot-mode examples (the harness at
`tests/actionlayer/run_batch` drives every `tests/actionlayer/scripts/*.json` case through
one-shot mode); there is no dedicated example harness for the daemon envelope shape at rest —
see `src/app/actiond/session_server.cpp`'s own `remapResult()` for the authoritative shape.

Every example response shown per-action below is the **one-shot-mode `result` entry's own
`"value"` field** (i.e. what a caller gets back on success), not the full envelope.

## Error handling

Every op returns a failure the same general way: `ok: false` (one-shot) / `status: "error"`
(daemon), with the failure detail in `error`. That `error` value is either a **plain string**
(most simple validation failures) or a **structured object** with at least `"type"` and
`"message"` fields (failures worth letting an automated/AI caller branch on programmatically).
Which shape a given failure uses is a property of *where* the check lives, not of severity:

- **Plain-string errors** come from a handler's own field validation, run *before* it resolves any
  name or calls into Seamly2D core -- e.g. `line` requiring non-empty `"firstPoint"`/`"secondPoint"`,
  `basePoint` requiring `"x"`/`"y"`/a non-empty `"draftBlock"` name, `endLine` requiring a `"length"`
  formula. These are deliberately terse ("X requires non-empty Y") rather than typed, since they are
  malformed-input errors a caller fixes once by reading the docs above, not errors a program needs
  to branch on by category.
- **Structured errors** (`{"type", "message", ...op-specific fields}`) cover every failure mode
  that *is* worth branching on:

  | `type`                     | Thrown/produced by                                                    | Extra fields |
  |-----------------------------|------------------------------------------------------------------------|--------------|
  | `nameResolution`            | `NameResolver::idForName()`/`nameForId()` (`ActionResolverError`, caught in `ActionEngine::run()`) | `name`, `kind` (`"notFound"` \| `"wrongScope"` \| `"duplicate"`), `knownNames`, `foundInScope` (only for `wrongScope`) |
  | `formulaError`              | A malformed formula string, caught from `qmu::QmuParserError` (see below) | `op`, `expr` (the sub-expression qmu was evaluating) — `point.edit`'s own two formula-editing branches instead build a shorter `{"type","message"}`-only object with no `op`/`expr` (see `point.edit` below) |
  | `draftBlockExists`          | `basePoint` naming a `draftBlock` that already exists (or is empty)   | `draftBlock` |
  | `noIntersection`            | `lineIntersect` naming two exactly-parallel lines                     | — |
  | `unsupported`               | `point.edit` naming a field that doesn't apply to the point's actual tool type | — |
  | `toolNotFound`              | `point.edit` naming a point with no registered tool (e.g. an operation-created destination point from `move`/`rotation`/`mirrorByLine`/`mirrorByAxis`) | — |
  | `invalidPiecePath`          | `piece.addPatternPiece`'s `"nodes"` not forming a closed, non-self-intersecting polygon | `nodes` (the echoed node-name array) |
  | `crossDraftBlockReference`  | `piece.addPatternPiece`/`piece.internalPath`/`piece.insertNodes`/`piece.addAnchorPoint` referencing a point (via `"nodes"` or `"point"`) that was created in a *different* draft block than the pattern's currently active one — see [Pieces](#pieces)'s own note on why this is checked at all | `offendingNodes` (the echoed name(s) that didn't match), `expectedDraftBlock` (the currently active one), `foundDraftBlock` (the mismatching one actually found) |
  | `unknownPiece`               | `piece.addAnchorPoint`/`piece.internalPath`/`piece.insertNodes`/`piece.union`/`piece.dump`/`render.snapshot` (`target: "piece"`) naming a piece that doesn't exist | `piece` |
  | `groupExists`                | `group` naming a group that already exists                            | `name` |
  | `missingMeasurements`       | `measurements.load`/`.sync`/`.recompute` against a file missing a measurement the pattern requires | `missing` (array of names) |
  | `measurementTypeMismatch`   | A measurement file supplying a value of the wrong type for a name the pattern expects | `expected`, `actual` |
  | `coreException`             | Any `VException` (or subclass: `VExceptionBadId`, `VExceptionWrongId`, `VExceptionObjectError`, `VExceptionEmptyParameter`, `VExceptionConversionError`, ...) that reaches `ActionEngine::run()`'s own catch clause uncaught by the handler itself | `detail` (only if the exception's own `DetailedInformation()` is non-empty) |
  | `unhandledException`        | Any other `std::exception` that reaches `ActionEngine::run()`'s catch clause uncaught | — |
  | `unknownError`               | A non-`std::exception` C++ exception (`catch (...)`) escaping a handler -- should never happen in practice | — |

  An "Unknown action op" failure (an `"op"` string absent from `ActionRegistry`) is its own case:
  a plain string, produced directly by `ActionEngine::run()` before any handler lookup happens at
  all, naming the offending op.

**Where `VException` handling actually lives.** Seamly2D core throws `VException` (and its
subclasses, `src/libs/ifc/exception/vexception*.h`) from tool-creation calls like
`VToolLine::Create()`/`VToolEndLine::Create()` on malformed pattern data (bad id, wrong object
type, missing DOM attribute, ...). Three layers guard against one ever reaching a caller as a raw
crash:

1. **Every handler that calls a real `Create()`** wraps that call in its own local
   `try { ... } catch (const VException&) catch (const std::exception&) catch (...)` (see
   `line_handlers.cpp`, `point_handlers.cpp`, `formula_point_handlers.cpp`'s shared `runCreate()`,
   and every other mutating handler), converting the failure into an `ActionResult::failure()`
   before it ever reaches `ActionEngine::run()`. This is the primary, expected path -- almost
   every `VException` a caller will ever see was caught here, not by layer 2.
2. **`ActionEngine::run()` itself** (`action_engine.cpp`) additionally catches
   `ActionResolverError`, `VException`, `std::exception`, and `...` around every handler
   dispatch, as a safety net for a handler added later without its own local catch. This is what
   produces the `coreException`/`unhandledException`/`unknownError` types above.
3. **The process-level catches** in `main.cpp` (one-shot mode, around the whole run) and
   `session_server.cpp`'s `processLine()` (daemon mode, around one request line) are the final,
   rarely-exercised nets -- they exist for a failure outside action dispatch entirely (a pattern
   file that fails to load/parse), not for a per-action failure, which layers 1-2 above already
   convert to a normal JSON result.

**Formula errors specifically:** `VAbstractTool::CheckFormula()`
(`src/libs/vtools/tools/vabstracttool.cpp`) evaluates every formula string via
`Calculator::EvalFormula()`. On a parse failure it checks `qApp->isAppInGUIMode()` -- `actiond`
(`actiond_application.cpp`) always reports `false`, so `CheckFormula()` never shows a "fix the
formula" dialog and always re-throws `qmu::QmuParserError` instead, which layer 1 above (each
formula-bearing handler's `runCreate()`) catches into the `formulaError` type. A broken formula is
therefore always a clean, fast JSON failure -- never a blocking dialog and never a process crash;
see `tests/actionlayer/scripts/07_error_cases.json` for a live example.

**Structured-error uniformity, verified during this refresh:** every handler file builds its
`{"type","message"}` structured errors via its own file-local `structuredError()` helper — the
exact same two-field shape, duplicated per file rather than shared (matching this codebase's
existing per-file-anonymous-namespace convention) — so every structured error in every category
below really is uniform, not just documented as if it were.

## Read-only introspection

### `pattern.dump`

**Maps to:** no `Create()` call — reads `VContainer::DataGObjects()` and
`VAbstractPattern::getHistory()` directly. `src/libs/actionlayer/handlers/pattern_dump_handler.cpp:151`

Takes no parameters.

**Example request:**
```json
{ "op": "pattern.dump" }
```

**Example success response:**
```json
{
  "objects": [
    { "id": 1, "name": "A", "type": "Point", "x": 0, "y": 0 },
    { "id": 2, "name": "B", "type": "Point", "x": 100, "y": 0,
      "formulaLength": "100", "formulaAngle": "0" }
  ],
  "history": [
    { "id": 1, "tool": "BasePoint", "draftBlock": "Front" },
    { "id": 2, "tool": "EndLine", "draftBlock": "Front" }
  ]
}
```
`objects` is sorted by id before being returned (fixed 20 Aug 2026 — see
[Findings](#findings)). `formulaLength`/`formulaAngle` only appear on a point created by a
`VToolLinePoint`-family tool (`endLine`/`alongLine`/`normal`/`bisector`/`shoulderPoint`) whose
own tool is still registered — an operation-created destination point (`move`/`rotation`/
`mirrorByLine`/`mirrorByAxis`) has no registered tool and so gets neither field, silently (not an
error — see the handler's own comment on why `VExceptionBadId` from `VAbstractPattern::getTool()`
is caught, not propagated, here).

**Known error cases:** none — always succeeds (an empty/unloaded context just yields empty arrays).

### `pattern.listMeasurements`

**Maps to:** no `Create()` call — reads `VContainer::DataMeasurements()` directly.
`src/libs/actionlayer/handlers/pattern_measurements_handler.cpp:39`

Takes no parameters.

**Example request:**
```json
{ "op": "pattern.listMeasurements" }
```

**Example success response:**
```json
{ "measurements": [ { "name": "shoulder_length", "formula": "13", "value": 13.0 } ] }
```
An empty array is a valid, non-error result for a pattern with no measurements attached.

**Known error cases:** none — always succeeds.

### `pattern.listTools`

**Maps to:** no `Create()` call — a hand-written static list.
`src/libs/actionlayer/handlers/pattern_list_tools_handler.cpp:37`

Takes no parameters. **Must be kept in sync by hand** with `action_registry.cpp`'s registration
list — it is not derived from the registry at runtime (its own header comment says so explicitly).
Currently lists all 51 registered ops correctly (was previously found out of sync with Phase 8's
additions and backfilled 20 Aug 2026, per its own in-file changelog comment; Phase 12, 22 Aug
2026, added the `pattern.undo` entry alongside registering it; 22 Aug 2026 added `piece.list`/
`piece.dump` alongside registering those).

**Example request:**
```json
{ "op": "pattern.listTools" }
```

**Example success response:**
```json
{ "tools": ["pattern.dump", "pattern.listMeasurements", "pattern.listTools", "render.snapshot", "..."] }
```

**Known error cases:** none — always succeeds.

### `pattern.resolveName`

**Maps to:** no `Create()` call — `NameResolver::idForName()` (unscoped overload).
`src/libs/actionlayer/handlers/pattern_resolve_name_handler.cpp:40`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | The user-visible name to resolve. |

Deliberately uses the **unscoped** `NameResolver::idForName(name, data)` overload, unlike every
mutating handler below (which all scope to `Draw::Calculation`) — this op's whole purpose is
introspection into the name space as a whole, including a name that only exists as a piece-node
clone (`Draw::Modeling`) or is genuinely ambiguous, so scoping it would defeat the point (see the
handler's own comment).

**Example request:**
```json
{ "op": "pattern.resolveName", "name": "A1" }
```

**Example success response:**
```json
{ "name": "A1", "id": 2, "type": "Point" }
```

**Known error cases:** `nameResolution` (unknown, ambiguous — `kind: "duplicate"` — or
wrong-scope name, though "wrong scope" cannot actually fire here since this op doesn't scope its
own lookup).

### `render.snapshot`

**Maps to:** no `Create()` call — rasterizes `ctx.scene()` (or, for `target: "piece"`, one
piece's own graphics item within `ctx.pieceScene()`) via `QPainter`/`QImage::save()`.
`src/libs/actionlayer/handlers/render_handlers.cpp:197`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `path` | string | yes | — | Output file path. Parent directory created if missing. |
| `target` | string | no (default `"draft"`) | — | Which scene to render. `"draft"` renders the whole draft scene, cropped to its content bounding box plus `padding` (the original behavior). `"piece"` renders a **tight crop of just one piece's own graphics item** — that item's `sceneBoundingRect()` plus `padding`, not `ctx.pieceScene()`'s own `itemsBoundingRect()`, which would include every other assembled piece too (pieces don't visually overlap there, each positioned at its own point coordinates, but rendering the whole scene would still show all of them at once) — requires `piece`. Anything else is a hard error, not a silent fallback. |
| `piece` | string | required when `target` is `"piece"`; ignored otherwise | — | Name or numeric id of an existing piece (see `piece.list`). A JSON string is matched against `VPiece::GetName()`; a JSON number is taken as a literal piece id and checked against `VContainer::DataPieces()`. |
| `format` | string | no | — | One of `PNG`/`JPG`/`BMP`/`TIF`/`PPM` (case-insensitive). Derived from `path`'s file extension if omitted; falls back to `PNG` if the extension is unrecognized/absent. |
| `background` | string | no | — | `"transparent"`, `"white"`, or a `QColor`-parsable string (e.g. `"#RRGGBB"`). Default depends on `format`: transparent for PNG/TIF/PPM, white for JPG/BMP (matching Seamly2D's own export functions' own per-format defaults). |
| `padding` | number | no (default `20.0`) | literal | Margin (scene units) added around the tight content bounding box on every side. |
| `width` / `height` | number | no | literal | Explicit pixel dimensions. Both given: used verbatim (may distort aspect ratio, by design). One given: the other is derived from the content's aspect ratio. Neither given: renders 1:1 scene-unit-to-pixel, capped so the larger dimension never exceeds 4096px (memory-safety cap; scaled down preserving aspect ratio if exceeded). |
| `highlight` | array of strings | no | — | Object names to overlay with a semi-transparent red rectangle. A name that fails to resolve, or resolves to an object with no live graphics item, is silently added to `skippedHighlights` (and logged via `qWarning`) rather than failing the whole render. |
| `showPointNames` | boolean | no | literal | Forces point-name labels (e.g. `"A1"`, `"A2"`) on (`true`) or off (`false`) for this render only, overriding the scene-wide "hide point names" setting for the duration of this one call (the previous value is restored afterward — this never permanently changes the pattern's/session's settings). A point created with its own `"showPointName": false` (see `basePoint`/`endLine`/etc.) still never shows its label even when this is `true` — this only controls the scene-wide toggle, not any individual point's own flag. Omitted: leaves the scene-wide setting exactly as-is. Works identically for both `target` values — this setting is scene-agnostic (a global `qApp->Settings()` flag every point's own paint code reads, regardless of which `QGraphicsScene` it happens to live in). **Note:** in the offscreen headless render path `actiond` always uses, labels only render as legible glyphs (not empty boxes) once a real font directory is available — `actiond`'s own `main.cpp` points `QT_QPA_FONTDIR` at the system font directory by default on Windows so this works out of the box; see that file's own comment if labels still render as boxes in a different environment. |

`highlight` is always resolved against `Draw::Calculation`-scoped names (the same names
`pattern.dump`'s `objects` array reports for the draft scene), so for `target: "piece"` a
resolved name's live graphics item is almost always in the *draft* scene, not the piece scene
actually being rendered — such a name is cleanly reported in `skippedHighlights` (not drawn at
the wrong location) rather than silently misplaced; see the handler's own comment on the
`item->scene() != scene` guard this relies on.

**Example request:**
```json
{ "op": "render.snapshot", "path": "square.png", "width": 400, "height": 400,
  "highlight": ["A", "B"], "showPointNames": true }
```

**Example request (`target: "piece"`):**
```json
{ "op": "render.snapshot", "path": "square_piece.png", "target": "piece", "piece": "Square" }
```

**Example success response:**
```json
{
  "op": "render.snapshot", "status": "ok",
  "path": "C:/.../square.png",
  "pixelSize": { "width": 400, "height": 400 },
  "boundingBox": { "x": -20.0, "y": -20.0, "width": 140.0, "height": 140.0 },
  "highlighted": ["A", "B"],
  "skippedHighlights": []
}
```
`piece` is only present in the response when `target` was `"piece"` — the resolved piece's own
name, so a caller who passed a numeric id can see what it named.

**Known error cases:** all plain-string except `unknownPiece` — `"unsupported target"` (anything
but `"draft"`/`"piece"`), missing/empty `"path"`, missing `"piece"` when `target` is `"piece"`,
`unknownPiece` (structured: `{"type": "unknownPiece", "message", "piece"}` — `target: "piece"`
naming a piece that doesn't exist, mirroring `piece.dump`'s own error shape), `"has no graphics
item to render"` (a resolved piece with no live `PatternPieceTool` instance — should not happen
in practice), `"unsupported format"`, `"unrecognized 'background' value"`, `"empty scene, nothing
to render"` (`target: "draft"` only — `scene->items().isEmpty()`; `target: "piece"` never reports
this, a resolved piece's own item always has real bounds), `"could not create output directory"`,
`"failed to save image to: <path>"` (`QImage::save()` returned false).

### `export.scene`

**Maps to:** no `Create()` call — writes `ctx.scene()` to a vector/DXF/raster device via
`QPainter` (`QSvgGenerator`/`QPrinter`/`VDxfPaintDevice`/`QImage::save()`), the same
render-the-current-draft-scene pattern `render.snapshot` uses, extended to every format
Seamly2D's GUI "Export Draft Blocks As" action supports (Phase A of the action-layer export
effort — nested cutting-layout export, `export.layout`/`export.pieces`, is a separate, not-yet-
implemented Phase B; see `docs/export-actions-notes.md`).
`src/libs/actionlayer/handlers/export_handlers.cpp:handleExportScene`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `path` | string | yes | — | Output file path. Parent directory created if missing. |
| `format` | string | no (required whenever `path` ends in `.dxf`) | — | One of `svg`, `pdf`, `ps`, `eps`, `png`, `jpg`, `bmp`, `ppm`, `tif`, `dxf-r10`, `dxf-r12`, `dxf-r13`, `dxf-r14`, `dxf-2000`, `dxf-2004`, `dxf-2007`, `dxf-2010`, `dxf-2013` (case-insensitive). Derived from `path`'s file extension if omitted; a bare `.dxf` extension spans nine AutoCAD versions and cannot be disambiguated, so `format` is mandatory there. An unrecognized/absent extension falls back to `png`. |
| `target` | string | no (default `"draft"`) | — | Same single-value guard as `render.snapshot` — `"draft"` is the only scene `ActionContext` exposes to this op today. |
| `padding` | number | no (default `20.0`) | literal | Same semantics as `render.snapshot`: margin (scene units) added around the tight content bounding box on every side. |
| `width` / `height` | number | no | literal | Explicit device dimensions — pixels for the five raster formats, scene units (the output's own unit) for every vector/DXF format. Both given: used verbatim (may distort aspect ratio, by design). One given: the other is derived from the content's aspect ratio. Neither given: renders 1:1 scene-unit-to-device-unit; for raster formats only, capped so the larger dimension never exceeds 4096px (a vector/DXF device never allocates a pixel buffer, so it is never capped). |
| `background` | string | no | — | Raster formats only (`png`/`jpg`/`bmp`/`ppm`/`tif`); silently unused for every other format. Same semantics as `render.snapshot`: `"transparent"`, `"white"`, or a `QColor`-parsable string. |
| `binaryDXF` | boolean | no (default `false`) | literal | Only meaningful for the nine `dxf-*` formats (maps to `VDxfPaintDevice::SetBinaryFormat`); silently unused otherwise. |
| `showPointNames` | boolean | no | literal | Same semantics as `render.snapshot`: forces point-name labels on/off for this export only. |

**Example request:**
```json
{ "op": "export.scene", "path": "square.svg" }
```

**Example success response:**
```json
{
  "op": "export.scene", "status": "ok",
  "path": "C:/.../square.svg",
  "format": "svg",
  "deviceSize": { "width": 140, "height": 140 },
  "boundingBox": { "x": -20.0, "y": -20.0, "width": 140.0, "height": 140.0 }
}
```

**Known error cases:** all plain-string — `"unsupported target"`, missing/empty `"path"`,
`"'format' is required when 'path' ends in .dxf"`, `"unsupported format"`, `"unrecognized
'background' value"`, `"empty scene, nothing to export"`, `"could not create output directory"`,
plus per-writer failures naming the path/reason (`"failed to open SVG/PDF/DXF output: <path>"`,
`"failed to save image to: <path>"`, `"could not start 'pdftops' ..."`, `"'pdftops' failed to
produce output: ..."`). **Partial coverage note:** `ps`/`eps` export shells out to the external
`pdftops` tool (Poppler/Xpdf) via `QProcess` — on a machine without it on `PATH`, those two
formats fail cleanly with a specific error rather than producing output; every other format has
no such external dependency.

## Points

### `basePoint`

**Maps to:** `VToolBasePoint::Create(...)` — `src/libs/vtools/tools/drawTools/toolpoint/toolsinglepoint/vtoolbasepoint.cpp:125` (declaration `vtoolbasepoint.h`)

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | The new point's name. |
| `x` / `y` | number | yes (both together) | **literal** | Coordinates, in `unit` (default mm). Converted via the free `ToPixel(value, unit)` function, not `qApp->toPixel()`, specifically so an explicit `"unit"` override works correctly (`qApp->toPixel()` alone only ever uses the loaded pattern's own declared unit). |
| `draftBlock` | string | yes | — | Name of the **new** draft block this point anchors. Must **not** already exist — `basePoint` only ever starts a brand-new draft block, mirroring `VToolBasePoint::AddToFile()`'s own unconditional "append a new `<draftBlock>`" behavior; there is no "reuse an existing block" path. |
| `mx` / `my` | number | no (default `0`) | literal | Label offset. |
| `unit` | string | no (default `"mm"`) | — | Unit `x`/`y` are expressed in (`"mm"`, `"cm"`, `"inch"`, ...; parsed via `StrToUnits()`). |

**Example request:**
```json
{ "op": "basePoint", "name": "A", "x": 0, "y": 0, "draftBlock": "Front" }
```

**Example success response:**
```json
{ "id": 1, "name": "A" }
```

**Known error cases:** plain strings for missing `name`/`x`/`y`/`draftBlock`; structured
`draftBlockExists` (`{"type","message","draftBlock"}`) if `draftBlock` already exists or is empty.

### `line`

**Maps to:** `VToolLine::Create(...)` — `src/libs/vtools/tools/drawTools/vtoolline.cpp:174` (declaration `vtoolline.h`)

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `firstPoint` / `secondPoint` | string | yes | — | Names of the line's two endpoints; both resolved as `Draw::Calculation` points. |
| `lineType` | string | no (default `solidLine`) | — | See the [`lineType` caveat](#findings) below. |
| `lineWeight` | string | no (default `0.35`) | — | |
| `lineColor` | string | no (default `black`) | — | |

Defaults traced from `DialogLine`'s constructor through `VCommonSettings::getDefaultLine{Type,
Weight,Color}()`'s own QSettings-missing fallbacks — the exact same literals a fresh Seamly2D
install's dialog would use.

**Example request:**
```json
{ "op": "line", "firstPoint": "A", "secondPoint": "B" }
```

**Example success response:**
```json
{ "id": 3, "firstPoint": "A", "secondPoint": "B" }
```

**Known error cases:** plain strings for missing endpoints or a resolved name that isn't a point
(`"\"firstPoint\" (\"X\") does not name a point"`); structured `nameResolution` for an unknown
name.

### `point.edit`

**Maps to:** no `Create()` call — mutates the named point's *existing* tool in place via each
tool class's own public setter, then that setter's own `VDrawTool::SaveOption()` write-and-recompute
path. `src/libs/actionlayer/handlers/point_edit_handlers.cpp:68`. Setters used:
`VToolBasePoint::SetBasePointPos()` (`vtoolbasepoint.h`), `VToolLinePoint::SetFormulaLength()`
(`vtoollinepoint.h`), `VToolEndLine::SetFormulaAngle()` (`vtoolendline.h`).

| Parameter | Type | Required | Literal / Formula | Applies to |
|---|---|---|---|---|
| `name` | string | yes | — | Any existing point. |
| `x` / `y` | number | no (both together, or neither) | **literal** | Only a `basePoint`-created point. Converted via `qApp->toPixel()` (the pattern's *current working unit*, no per-call override — unlike `basePoint`'s own `x`/`y`, which accept an explicit `"unit"`). |
| `length` | string | no | **formula** | Any `VToolLinePoint`-family point (`endLine`/`alongLine`/`normal`/`bisector`/`shoulderPoint`). Zero-length formulas are rejected (`setCheckZero(true)`). |
| `angle` | string | no | **formula** | Only an `endLine`-created point today — `normal`/`bisector`/`shoulderPoint` use a plain numeric angle *offset* (not a formula, no public formula-angle setter); `curveIntersectAxis` has its own formula-angle setter this op does not reach yet. Documented follow-up, not a silent gap. Zero angle is valid (`setCheckZero(false)`). |

At least one of `x`/`y`, `length`, or `angle` is required; `x` and `y` must be given together, not
alone.

**Example request:**
```json
{ "op": "point.edit", "name": "D", "length": "0" }
```

**Example success response:**
```json
{ "id": 4, "name": "D", "updated": ["length"] }
```
`"updated"` lists exactly which of `x`/`y`/`length`/`angle` were actually applied (`x` and `y`
both appear together when given).

**Known error cases:** plain strings for missing `name` or `x`-without-`y`(-or-vice-versa) or no
fields at all; structured `nameResolution` for an unknown name; structured `toolNotFound`
(**new in this refresh — was missing from the error-type table**) if the named point has no
registered tool at all (e.g. an operation-created destination point); structured `unsupported` if
a given field doesn't apply to the point's actual tool type; structured `formulaError` for a bad
`length`/`angle` formula (this handler's own two `formulaError` objects are `{"type","message"}`
only — no `op`/`expr` fields, unlike every `Create()`-based handler's `runCreate()`-produced
`formulaError`; see [Findings](#findings)).

## Formula-bearing points

Every op below (except `lineIntersect`, which has no formula at all) passes its formula field(s)
straight into the matching `VToolXxx::Create()` overload as a raw `QString`, never pre-evaluated —
`VAbstractTool::CheckFormula()` (called *inside* `Create()`) is what actually evaluates it, against
`data->DataVariables()`, so the point stays measurement-reactive. All six resolve every point/line
name via `NameResolver::idForName(name, data, Draw::Calculation)`.

### `endLine`

**Maps to:** `VToolEndLine::Create(...)` — `src/libs/vtools/tools/drawTools/toolpoint/toolsinglepoint/toollinepoint/vtoolendline.cpp:178`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `basePoint` | string | yes | — | |
| `length` | string | yes | **formula** | |
| `angle` | string | no (default `"0"`) | **formula** | Unlike `normal`'s `angle` (a plain number), `endLine`'s is a real formula string. |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | Same defaults as `line`. |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "endLine", "name": "B", "basePoint": "A", "length": "100", "angle": "0" }
```

**Example success response:**
```json
{ "id": 2, "name": "B", "op": "endLine" }
```

**Known error cases:** plain strings for missing `name`/`basePoint`/`length`; structured
`nameResolution`; structured `formulaError` (`{"type","message","op","expr"}`).

### `alongLine`

**Maps to:** `VToolAlongLine::Create(...)` — `.../toollinepoint/vtoolalongline.cpp:314`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `firstPoint` / `secondPoint` | string | yes | — | |
| `length` | string | yes | **formula** | Distance from `firstPoint` along the line toward `secondPoint`. |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "alongLine", "name": "M", "firstPoint": "A", "secondPoint": "B", "length": "50" }
```

**Example success response:**
```json
{ "id": 5, "name": "M", "op": "alongLine" }
```

**Known error cases:** same shape family as `endLine`.

### `normal`

**Maps to:** `VToolNormal::Create(...)` — `.../toollinepoint/vtoolnormal.cpp:180`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `firstPoint` / `secondPoint` | string | yes | — | |
| `length` | string | yes | **formula** | Distance along the normal to the `firstPoint`–`secondPoint` line. |
| `angle` | number **or** numeric string | no (default `0`) | **literal, not a formula** | Rotation offset — `VToolNormal::Create()`'s own `const qreal angle` parameter. Read via `.toVariant().toDouble()` specifically so a JSON string like `"0"` *and* a bare JSON number both work (`QJsonValue::toDouble()` alone would silently default for a string). This is the one field in this whole file family that looks like the others but is genuinely never passed through `CheckFormula`/qmuparser. |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "normal", "name": "N", "firstPoint": "A", "secondPoint": "B", "length": "20", "angle": 0 }
```

**Example success response:**
```json
{ "id": 6, "name": "N", "op": "normal" }
```

**Known error cases:** same shape family as `endLine` (`angle` itself can never produce a
`formulaError`, since it never reaches `CheckFormula`).

### `bisector`

**Maps to:** `VToolBisector::Create(...)` — `.../toollinepoint/vtoolbisector.cpp:215`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `firstPoint` / `secondPoint` / `thirdPoint` | string | yes | — | Bisector of the angle at `secondPoint`, between rays to `firstPoint` and `thirdPoint`. |
| `length` | string | yes | **formula** | |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "bisector", "name": "Bi", "firstPoint": "A", "secondPoint": "B", "thirdPoint": "C", "length": "20" }
```

**Example success response:**
```json
{ "id": 7, "name": "Bi", "op": "bisector" }
```

**Known error cases:** same shape family as `endLine`.

### `shoulderPoint`

**Maps to:** `VToolShoulderPoint::Create(...)` — `.../toollinepoint/vtoolshoulderpoint.cpp:227`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `p1Line` / `p2Line` | string | yes | — | The two points defining the line the new point projects onto. |
| `pShoulder` | string | yes | — | The "shoulder" point the length is measured from. |
| `length` | string | yes | **formula** | |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "shoulderPoint", "name": "Sh", "p1Line": "A", "p2Line": "B", "pShoulder": "C", "length": "20" }
```

**Example success response:**
```json
{ "id": 8, "name": "Sh", "op": "shoulderPoint" }
```

**Known error cases:** same shape family as `endLine`.

### `lineIntersect`

**Maps to:** `VToolLineIntersect::Create(...)` — `.../toolsinglepoint/vtoollineintersect.cpp:170`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `p1Line1` / `p2Line1` | string | yes | — | First line's two endpoints. |
| `p1Line2` / `p2Line2` | string | yes | — | Second line's two endpoints. |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

The only one of these six ops with **no formula field at all** — pure geometric intersection.

**Example request:**
```json
{ "op": "lineIntersect", "name": "X", "p1Line1": "A", "p2Line1": "B", "p1Line2": "C", "p2Line2": "D" }
```

**Example success response:**
```json
{ "id": 9, "name": "X", "op": "lineIntersect" }
```

**Known error cases:** plain strings for missing fields; structured `nameResolution`; structured
`noIntersection` (`{"type","message"}`) when the two lines are exactly parallel —
`VToolLineIntersect::Create()` returns `nullptr` in that case (a real, reachable outcome here,
unlike every other op's defensive-only nullptr guard).

## Curves

All resolve point/curve names via `NameResolver::idForName(..., Draw::Calculation)`.
`spline`/`arc`/`arcWithLength`/`ellipticalArc`/`splinePath` pass their formula fields straight
through, unevaluated, exactly like the point-formula family above. `cubicBezier`/
`cubicBezierPath` have **no formula-based `Create()` overload at all** — a cubic bezier's shape is
defined directly by its named control points — so those two build the raw geometry object
(`VCubicBezier`/`VCubicBezierPath`) and use its id+object `Create()` overload instead, the same
idiom `basePoint` uses for `VPointF`.

### `spline`

**Maps to:** `VToolSpline::Create(...)` — `src/libs/vtools/tools/drawTools/toolcurve/vtoolspline.cpp:237`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `point1` / `point4` | string | yes | — | Endpoints. |
| `length1` / `length2` | string | yes | **formula** | Tangent-handle lengths at `point1`/`point4`. |
| `angle1` / `angle2` | string | no (default `"0"`) | **formula** | Tangent angles at `point1`/`point4`. |
| `duplicate` | integer | no (default `0`) | literal | |
| `lineColor` | string | no | — | |
| `lineType` | string | no | — | Maps onto `Create()`'s `penStyle` parameter — named `"lineType"` in JSON for consistency with every other curve/line op, not a mismatch. |
| `lineWeight` | string | no | — | |
| `autoSmooth` | bool | no (default `false`) | literal | |
| `lengthMode` | integer | no (default `0`) | literal | |
| `targetLength` | string | no (default `""`) | — | |

**Example request:**
```json
{ "op": "spline", "point1": "A", "point4": "B", "angle1": "0", "angle2": "180", "length1": "30", "length2": "30" }
```

**Example success response:**
```json
{ "id": 10, "point1": "A", "point4": "B", "op": "spline" }
```

**Known error cases:** plain strings for missing endpoints/`length1`/`length2`; structured
`nameResolution`; structured `formulaError`.

### `splinePath`

**Maps to:** `VToolSplinePath::Create(...)` — `.../toolcurve/vtoolsplinepath.cpp:233`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `points` | array of objects | yes (≥2 entries) | — | Each entry: `{"point","angle1"?,"angle2"?,"length1","length2"}`. `angle1`/`angle2` default `"0"` per entry; `length1`/`length2` required per entry. One quad of formulas **per point**, not per segment — matches `VToolSplinePath::Create()`'s raw overload signature (`QVector<QString> a1/a2/l1/l2`, one entry per point). |
| `duplicate` | integer | no (default `0`) | literal | Action-level, not per-point. |
| `lineColor` / `lineType` / `lineWeight` | string | no | — | Action-level, not per-point. |

**Example request:**
```json
{ "op": "splinePath", "points": [
    { "point": "A", "angle1": "0", "angle2": "180", "length1": "30", "length2": "30" },
    { "point": "B", "angle1": "0", "angle2": "180", "length1": "30", "length2": "30" }
] }
```

**Example success response:**
```json
{ "id": 11, "points": ["A", "B"], "op": "splinePath" }
```

**Known error cases:** plain strings for `<2` entries, an entry with an empty `"point"`, or an
entry missing `length1`/`length2`; structured `nameResolution`; structured `formulaError`.

### `cubicBezier`

**Maps to:** `VToolCubicBezier::Create(...)` (id+object overload) — `.../toolcurve/vtoolcubicbezier.cpp:170`. No formula-based overload exists for this tool.

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `point1` / `point4` | string | yes | — | Curve endpoints. |
| `point2` / `point3` | string | yes | — | Control points. |
| `lineColor` / `lineType` / `lineWeight` | string | no | — | |

**Example request:**
```json
{ "op": "cubicBezier", "point1": "A", "point2": "CP1", "point3": "CP2", "point4": "B" }
```

**Example success response:**
```json
{ "id": 12, "point1": "A", "point4": "B", "op": "cubicBezier" }
```

**Known error cases:** plain string for missing/non-point names; structured `nameResolution`. No
`formulaError` is possible for this op — no formula field exists.

### `cubicBezierPath`

**Maps to:** `VToolCubicBezierPath::Create(...)` (id+object overload) — `.../toolcurve/vtoolcubicbezierpath.cpp:135`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `points` | array of strings | yes (4, 7, 10, ... entries: `(n-4) % 3 == 0`) | — | Flat list of already-named points: 4 for one segment, then 3 more per additional segment (each extra segment reuses the previous segment's last point as its own start). |
| `lineColor` / `lineType` / `lineWeight` | string | no | — | |

**Example request:**
```json
{ "op": "cubicBezierPath", "points": ["A", "CP1", "CP2", "B", "CP3", "CP4", "C"] }
```

**Example success response:**
```json
{ "id": 13, "points": ["A", "CP1", "CP2", "B", "CP3", "CP4", "C"], "op": "cubicBezierPath" }
```

**Known error cases:** plain string for a wrong-count/non-point `points` array; structured
`nameResolution`. No `formulaError` possible.

### `arc`

**Maps to:** `VToolArc::Create(...)` — `.../toolcurve/vtoolarc.cpp:165`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `center` | string | yes | — | |
| `radius` | string | yes | **formula** | |
| `f1` / `f2` | string | yes | **formula** | Start/end angles. |
| `lineColor` / `lineType` / `lineWeight` | string | no | — | |

**Example request:**
```json
{ "op": "arc", "center": "A", "radius": "50", "f1": "0", "f2": "90" }
```

**Example success response:**
```json
{ "id": 14, "center": "A", "op": "arc" }
```

**Known error cases:** plain strings for missing `center`/`radius`/`f1`/`f2`; structured
`nameResolution`; structured `formulaError`.

### `arcWithLength`

**Maps to:** `VToolArcWithLength::Create(...)` — `.../toolcurve/vtoolarcwithlength.cpp:136`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `center` | string | yes | — | |
| `radius` | string | yes | **formula** | |
| `f1` | string | yes | **formula** | Start angle. |
| `length` | string | yes | **formula** | Arc length (instead of a second angle `f2`). |
| `lineColor` / `lineType` / `lineWeight` | string | no | — | |

**Example request:**
```json
{ "op": "arcWithLength", "center": "A", "radius": "50", "f1": "0", "length": "78.5" }
```

**Example success response:**
```json
{ "id": 15, "center": "A", "op": "arcWithLength" }
```

**Known error cases:** same shape family as `arc`.

### `ellipticalArc`

**Maps to:** `VToolEllipticalArc::Create(...)` — `.../toolcurve/vtoolellipticalarc.cpp:168`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `center` | string | yes | — | |
| `radius1` / `radius2` | string | yes | **formula** | |
| `f1` / `f2` | string | yes | **formula** | Start/end angles. |
| `rotationAngle` | string | no (default `"0"`) | **formula** | |
| `lineColor` / `lineType` / `lineWeight` | string | no | — | |

**Example request:**
```json
{ "op": "ellipticalArc", "center": "A", "radius1": "60", "radius2": "30", "f1": "0", "f2": "180" }
```

**Example success response:**
```json
{ "id": 16, "center": "A", "op": "ellipticalArc" }
```

**Known error cases:** same shape family as `arc`.

## Cut / intersection points

All ten resolve names via `NameResolver::idForName(..., Draw::Calculation)`. Three of the ten
(`pointOfIntersectionArcs`, `pointFromArcAndTangent`, `triangle`) involve no formula at all — the
same way `lineIntersect` does not. `direction`/`crossPoint`/`vCrossPoint`/`hCrossPoint` are all
plain disambiguation strings read directly (not formulas).

### `cutSpline`

**Maps to:** `VToolCutSpline::Create(...)` — `.../toolcut/vtoolcutspline.cpp:156`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `curve` | string | yes | — | Any of spline/splinePath/cubicBezier/cubicBezierPath (`VAbstractCubicBezier`-derived — verified against the handler's own `checkIsCubicBezierCurve()`, which checks exactly those four `GOType`s, not literally `GOType::Spline` only). |
| `direction` | string | yes | — | `"forward"` or anything else (treated as `"backward"` — matches `Create()`'s own `direction == "forward"` check exactly; there is no validation rejecting a third value, only a presence check). |
| `length` | string | yes | **formula** | Distance from the chosen end. |
| `lineColor` | string | no | — | |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "cutSpline", "name": "Cut1", "curve": "Curve1", "direction": "forward", "length": "50" }
```

**Example success response:**
```json
{ "id": 17, "name": "Cut1", "op": "cutSpline" }
```

**Known error cases:** plain strings for missing fields / wrong-type `curve`; structured
`nameResolution`; structured `formulaError`.

### `cutArc`

**Maps to:** `VToolCutArc::Create(...)` — `.../toolcut/vtoolcutarc.cpp:153`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `arc` | string | yes | — | Must resolve to `GOType::Arc`. |
| `direction` | string | yes | — | Same `"forward"`/else contract as `cutSpline`. |
| `length` | string | yes | **formula** | |
| `lineColor` | string | no | — | |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "cutArc", "name": "Cut2", "arc": "Arc1", "direction": "forward", "length": "30" }
```

**Example success response:**
```json
{ "id": 18, "name": "Cut2", "op": "cutArc" }
```

**Known error cases:** same shape family as `cutSpline`.

### `pointOfIntersectionArcs`

**Maps to:** `VToolPointOfIntersectionArcs::Create(...)` — `.../toolsinglepoint/vtoolpointofintersectionarcs.cpp:129`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `firstArc` / `secondArc` | string | yes | — | Both must resolve to `GOType::Arc`. |
| `crossPoint` | string | yes | — | `"firstPoint"` or `"secondPoint"` (case-insensitive); any other value is a plain-string error naming what was actually received. |
| `mx` / `my` | number | no (default `0`) | literal | |

No `showPointName` field — `VToolPointOfIntersectionArcs::Create()`'s raw overload has no such
parameter (unlike most of its siblings).

**Example request:**
```json
{ "op": "pointOfIntersectionArcs", "name": "X1", "firstArc": "Arc1", "secondArc": "Arc2", "crossPoint": "firstPoint" }
```

**Example success response:**
```json
{ "id": 19, "name": "X1", "op": "pointOfIntersectionArcs" }
```

**Known error cases:** plain strings for missing fields, wrong-type arcs, or an invalid
`crossPoint`; structured `nameResolution`. No formula involved.

### `pointOfIntersectionCircles`

**Maps to:** `IntersectCirclesTool::Create(...)` — `.../toolsinglepoint/intersect_circles_tool.cpp:141`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `firstCircleCenter` / `secondCircleCenter` | string | yes | — | Point names. |
| `firstCircleRadius` / `secondCircleRadius` | string | yes | **formula** | |
| `crossPoint` | string | yes | — | Same `"firstPoint"`/`"secondPoint"` contract as `pointOfIntersectionArcs`. |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "pointOfIntersectionCircles", "name": "X2", "firstCircleCenter": "A", "secondCircleCenter": "B",
  "firstCircleRadius": "50", "secondCircleRadius": "60", "crossPoint": "firstPoint" }
```

**Example success response:**
```json
{ "id": 20, "name": "X2", "op": "pointOfIntersectionCircles" }
```

**Known error cases:** plain strings for missing fields/invalid `crossPoint`; structured
`nameResolution`; structured `formulaError` (for the two radius formulas).

### `pointOfIntersectionCurves`

**Maps to:** `VToolPointOfIntersectionCurves::Create(...)` — `.../toolsinglepoint/vtoolpointofintersectioncurves.cpp:129`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `firstCurve` / `secondCurve` | string | yes | — | Any curve type (arc, elliptical arc, spline, splinePath, cubicBezier, cubicBezierPath). |
| `vCrossPoint` | string | yes | — | `"highestPoint"` or `"lowestPoint"` (case-insensitive). |
| `hCrossPoint` | string | yes | — | `"leftmostPoint"` or `"rightmostPoint"` (case-insensitive). |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "pointOfIntersectionCurves", "name": "X3", "firstCurve": "Curve1", "secondCurve": "Curve2",
  "vCrossPoint": "highestPoint", "hCrossPoint": "leftmostPoint" }
```

**Example success response:**
```json
{ "id": 21, "name": "X3", "op": "pointOfIntersectionCurves" }
```

**Known error cases:** plain strings for missing/wrong-type curve names or invalid
`vCrossPoint`/`hCrossPoint`; structured `nameResolution`. No formula involved.

### `curveIntersectAxis`

**Maps to:** `VToolCurveIntersectAxis::Create(...)` — `.../toollinepoint/vtoolcurveintersectaxis.cpp:151`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `basePoint` | string | yes | — | Ray origin. |
| `curve` | string | yes | — | Any curve type, **including an arc** — this op also covers what the design plan separately called "arcIntersectAxis"; there is no separate handler because `Tool::ArcIntersectAxis` is a reserved enum value with no live tool class in this codebase (confirmed against `pattern_dump_handler.cpp`'s own `toolToString()` comment). |
| `angle` | string | yes | **formula** | Ray angle from `basePoint`. |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "curveIntersectAxis", "name": "X4", "basePoint": "A", "curve": "Curve1", "angle": "45" }
```

**Example success response:**
```json
{ "id": 22, "name": "X4", "op": "curveIntersectAxis" }
```

**Known error cases:** plain strings for missing fields/wrong-type names; structured
`nameResolution`; structured `formulaError`.

### `pointFromCircleAndTangent`

**Maps to:** `IntersectCircleTangentTool::Create(...)` — `.../toolsinglepoint/intersect_circletangent_tool.cpp:132`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `circleCenter` | string | yes | — | |
| `circleRadius` | string | yes | **formula** | |
| `tangentPoint` | string | yes | — | External point the tangent line passes through. |
| `crossPoint` | string | yes | — | `"firstPoint"`/`"secondPoint"`. |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

**Example request:**
```json
{ "op": "pointFromCircleAndTangent", "name": "T1", "circleCenter": "A", "circleRadius": "40",
  "tangentPoint": "B", "crossPoint": "firstPoint" }
```

**Example success response:**
```json
{ "id": 23, "name": "T1", "op": "pointFromCircleAndTangent" }
```

**Known error cases:** plain strings for missing fields/invalid `crossPoint`; structured
`nameResolution`; structured `formulaError`.

### `pointFromArcAndTangent`

**Maps to:** `VToolPointFromArcAndTangent::Create(...)` — `.../toolsinglepoint/vtoolpointfromarcandtangent.cpp:132`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `arc` | string | yes | — | |
| `tangentPoint` | string | yes | — | |
| `crossPoint` | string | yes | — | `"firstPoint"`/`"secondPoint"`. |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

No formula field at all.

**Example request:**
```json
{ "op": "pointFromArcAndTangent", "name": "T2", "arc": "Arc1", "tangentPoint": "B", "crossPoint": "firstPoint" }
```

**Example success response:**
```json
{ "id": 24, "name": "T2", "op": "pointFromArcAndTangent" }
```

**Known error cases:** plain strings for missing fields/invalid `crossPoint`; structured
`nameResolution`. No `formulaError` possible.

### `triangle`

**Maps to:** `VToolTriangle::Create(...)` — `.../toolsinglepoint/vtooltriangle.cpp:170`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `axisP1` / `axisP2` | string | yes | — | The axis line the result point lies on. |
| `firstPoint` / `secondPoint` | string | yes | — | The hypotenuse's two ends; the result is where a right angle forms on the axis. |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

No formula field at all.

**Example request:**
```json
{ "op": "triangle", "name": "Tr", "axisP1": "A", "axisP2": "B", "firstPoint": "C", "secondPoint": "D" }
```

**Example success response:**
```json
{ "id": 25, "name": "Tr", "op": "triangle" }
```

**Known error cases:** plain strings for missing/wrong-type point names; structured
`nameResolution`. No `formulaError` possible.

### `height`

**Maps to:** `VToolHeight::Create(...)` — `.../toollinepoint/vtoolheight.cpp:175`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `basePoint` | string | yes | — | Point being projected. |
| `p1Line` / `p2Line` | string | yes | — | The line `basePoint` is projected onto (perpendicular). |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | |
| `mx` / `my` | number | no (default `0`) | literal | |
| `showPointName` | bool | no (default `true`) | literal | |

No formula field at all.

**Example request:**
```json
{ "op": "height", "name": "H", "basePoint": "C", "p1Line": "A", "p2Line": "B" }
```

**Example success response:**
```json
{ "id": 26, "name": "H", "op": "height" }
```

**Known error cases:** plain strings for missing/wrong-type point names; structured
`nameResolution`. No `formulaError` possible.

## Operations

`move`/`rotation`/`mirrorByLine`/`mirrorByAxis` all take `"sourceObjects"` (a JSON array of names —
**any geometry type**, not just points: these tools transform whole lines/arcs/curves, verified
against `resolveSourceObjects()`, which applies no type filter at all beyond resolving each name)
and a required `"suffix"`, and all recover their created-object list the same way: since
`VAbstractOperation`'s own `destination` vector is a protected member with no public getter, the
handler instead diffs `VContainer::DataGObjects()`'s id set before/after the `Create()` call and
sorts the new ids ascending — safe because every one of these four `Create()` overloads shares the
same "allocate ids via `VContainer::getNextId()` in `source` order" loop (verified against
`VToolMove::Create()`/`VToolRotation::Create()` directly), so ascending-sorted new ids line up with
input order.

### `move`

**Maps to:** `VToolMove::Create(...)` — `src/libs/vtools/tools/drawTools/operation/vtoolmove.cpp:201`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `sourceObjects` | array of strings | yes (non-empty) | — | |
| `suffix` | string | yes | — | Appended to each source name to name the new object (`"A1"` + `"_m"` → `"A1_m"`). |
| `length` / `angle` / `rotationAngle` | string | yes (all three) | **formula** | |
| `rotationOrigin` | string | no | — | A point name. Omitted: `VToolMove::Create()` derives the rotation origin from the source objects' own centroid (`VAbstractOperation::findRotationOrigin()`). |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | Applied uniformly to every source item (no per-item override in this JSON shape). |

**Example request:**
```json
{ "op": "move", "sourceObjects": ["A", "B"], "suffix": "_m", "length": "20", "angle": "0", "rotationAngle": "0" }
```

**Example success response:**
```json
{ "created": [ { "id": 27, "name": "A_m" }, { "id": 28, "name": "B_m" } ], "op": "move" }
```

**Known error cases:** plain strings for missing `suffix`/formulas/empty `sourceObjects`; structured
`nameResolution`; structured `formulaError`.

### `rotation`

**Maps to:** `VToolRotation::Create(...)` — `.../operation/vtoolrotation.cpp:149`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `sourceObjects` | array of strings | yes | — | |
| `suffix` | string | yes | — | |
| `origin` | string | yes | — | A point name — **required** here (unlike `move`'s optional `rotationOrigin`; `VToolRotation::Create()`'s `origin` parameter has no derived-origin path). |
| `angle` | string | yes | **formula** | |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | |

**Example request:**
```json
{ "op": "rotation", "sourceObjects": ["A", "B"], "suffix": "_r", "origin": "A", "angle": "90" }
```

**Example success response:**
```json
{ "created": [ { "id": 29, "name": "A_r" }, { "id": 30, "name": "B_r" } ], "op": "rotation" }
```

**Known error cases:** same shape family as `move`.

### `mirrorByLine`

**Maps to:** `VToolMirrorByLine::Create(...)` — `.../operation/mirror/vtoolmirrorbyline.cpp:123`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `sourceObjects` | array of strings | yes | — | |
| `suffix` | string | yes | — | |
| `firstLinePoint` / `secondLinePoint` | string | yes | — | The mirror line's two defining points. No formula involved — a mirror line is geometric, not formula-derived. |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | |

**Example request:**
```json
{ "op": "mirrorByLine", "sourceObjects": ["A", "B"], "suffix": "_mir", "firstLinePoint": "A", "secondLinePoint": "B" }
```

**Example success response:**
```json
{ "created": [ { "id": 31, "name": "A_mir" }, { "id": 32, "name": "B_mir" } ], "op": "mirrorByLine" }
```

**Known error cases:** plain strings for missing fields/wrong-type points; structured
`nameResolution`. No `formulaError` possible.

### `mirrorByAxis`

**Maps to:** `VToolMirrorByAxis::Create(...)` — `.../operation/mirror/vtoolmirrorbyaxis.cpp:122`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `sourceObjects` | array of strings | yes | — | |
| `suffix` | string | yes | — | |
| `originPoint` | string | yes | — | |
| `axisType` | string | yes | — | `"vertical"` or `"horizontal"` (case-insensitive); anything else is a plain-string error naming what was received. |
| `lineType` / `lineWeight` / `lineColor` | string | no | — | |

**Example request:**
```json
{ "op": "mirrorByAxis", "sourceObjects": ["A", "B"], "suffix": "_ax", "originPoint": "A", "axisType": "vertical" }
```

**Example success response:**
```json
{ "created": [ { "id": 33, "name": "A_ax" }, { "id": 34, "name": "B_ax" } ], "op": "mirrorByAxis" }
```

**Known error cases:** plain strings for missing fields/invalid `axisType`/wrong-type point;
structured `nameResolution`. No `formulaError` possible.

### `group`

**Maps to:** no `VTool::Create()` at all — pure `VAbstractPattern` (doc) DOM bookkeeping via
`doc->createGroup()`/`createGroups()`/`parseGroups()`, reproducing `AddGroup::redo()`'s
(`src/libs/vtools/undocommands/addgroup.cpp`) non-undo-stack logic directly (every mutating
handler in this action layer calls straight through without pushing an undo command — this one
just has no `VTool::Create()` step to call through at all).
`src/libs/actionlayer/handlers/operation_handlers.cpp:475`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Must not already exist. |
| `sourceObjects` | array of strings | yes (non-empty) | — | Each entry's resolved id is recorded under the group as **both** the map's tool-id and object-id key — the simple case every non-composite selected object uses in the interactive group dialog. A source object that is itself part of a multi-part tool (e.g. one endpoint of a two-point tool) is not separately addressable via this action layer yet — a documented gap, not a silent mismatch. |
| `color` / `lineType` / `lineWeight` | string | no | — | |

**Example request:**
```json
{ "op": "group", "name": "Notches", "sourceObjects": ["A", "B"] }
```

**Example success response — undocumented until this refresh (see [Findings](#findings)):**
```json
{ "id": 35, "name": "Notches", "sourceObjects": ["A", "B"], "op": "group" }
```
Note this is **not** the `{"created":[...],"op"}` shape every other operation in this section
returns — `group` creates no new geometry, so there is nothing to list in a `"created"` array.

**Known error cases:** plain strings for missing `name`/empty `sourceObjects`/a non-string entry;
structured `nameResolution`; structured `groupExists` (**new in this refresh — was missing from
the error-type table**, `{"type","message","name"}`) if the group name already exists.

### `trueDarts`

**Maps to:** `VToolTrueDarts::Create(...)` — `.../toolpoint/tooldoublepoint/vtooltruedarts.cpp:171`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `point1Name` / `point2Name` | string | yes | — | Names for the two **new** result points (not existing names to resolve — these are output names). |
| `baseLineP1` / `baseLineP2` | string | yes | — | The dart's base line. |
| `dartP1` / `dartP2` / `dartP3` | string | yes | — | The dart's three defining points. |
| `mx1` / `my1` | number | no (default `0`) | literal | Label offset for the first result point. |
| `showPointName1` | bool | no (default `true`) | literal | |
| `mx2` / `my2` | number | no (default `0`) | literal | Label offset for the second result point. |
| `showPointName2` | bool | no (default `true`) | literal | |

No formula field — finds the dart's true (folded) positions geometrically.

**Example request:**
```json
{ "op": "trueDarts", "point1Name": "T1", "point2Name": "T2", "baseLineP1": "A", "baseLineP2": "B",
  "dartP1": "D1", "dartP2": "D2", "dartP3": "D3" }
```

**Example success response:**
```json
{ "id": 36, "point1Name": "T1", "point2Name": "T2", "op": "trueDarts" }
```
Unlike `move`/`rotation`/`mirrorByLine`/`mirrorByAxis`, this returns a single `"id"` (`Create()`'s
own tool id) plus the two echoed output names — **not** a `"created"` array (verified against the
handler's actual payload-building code, matching what the design plan predicted).

**Known error cases:** plain strings for missing/wrong-type point names; structured
`nameResolution`. No `formulaError` possible.

## Pieces

Unlike every handler above, these do real assembly work, not thin JSON-to-`Create()` translation
— see [Findings](#findings) for what that means for the "handlers are thin translators" rule this
codebase otherwise holds to. All use `ctx.pieceScene()` (not `ctx.scene()`) for the three ops that
take a scene at all. Only point-type nodes are currently supported in every `"nodes"` array below
(`Tool::NodePoint`) — arc/curve piece nodes are a documented follow-up gap, not a silent mismatch.

**Piece-node cloning, verified during this refresh:** `piece.addPatternPiece`/
`piece.internalPath` cannot pass an original draft point's id directly into a `VPieceNode` —
`VAbstractTool::PrepareNode()` (which every *dialog*-based `Create()` overload calls first) clones
the point into a fresh `VContainer` object and registers a `VNodePoint` wrapping the clone; only
that clone's id is a valid piece-path node id. Since `PrepareNode()` is `protected`, these two
handlers reproduce its exact two-call body (`VAbstractTool::CreateNode<VPointF>()` +
`VNodePoint::Create()`) themselves, then rename the clone to `"__pieceNode_<id>"` as a
defense-in-depth measure against a real, found-and-fixed name-collision bug (see
`piece_handlers.h`'s own "FOUND AND FIXED" comment, dated 20 Aug 2026, for the full incident —
`NameResolver::idForName()`'s `Draw`-scoped overload is the actual fix; the rename is now a
second, independent safety net). `piece.insertNodes` is the one op in this file that does **not**
need this cloning step, since `PatternPieceTool::insertNodes()` calls `PrepareNode()` itself
internally.

**Cross-draft-block reference validation (found and fixed, 22 Aug 2026):** every point
`piece.addPatternPiece`/`piece.internalPath`/`piece.insertNodes` reference via `"nodes"`, and the
one `piece.addAnchorPoint` references via `"point"`, must belong to the pattern's **currently
active draft block** (`VAbstractPattern::getActiveDraftBlockName()`) — a referenced point from a
*different*, earlier draft block is rejected with a structured `crossDraftBlockReference` error
(see the error-type table above) rather than silently succeeding. **Do not "fix" this validation
away as overly strict** — it exists because `VPattern::parseDraftBlockElement()`
(`src/app/seamly2d/xml/vpattern.cpp`) unconditionally wipes *every* previously-parsed draft
block's calculation-scope objects (`VContainer::ClearCalculationGObjects()`) each time a later
draft block's own `<calculation>` section is (re)parsed — correct, intentional, shared behavior
(draft blocks are self-contained calculation scopes, never meant to interlink; see [`basePoint`](#basepoint)'s
own entry above on why it always starts a brand-new block). A piece/anchor referencing a point
from a different block succeeds silently in a live session (`VContainer` is only wiped during file
*parsing*, never mid-session) but corrupts the saved file: on reload, whichever draft block parses
*last* wipes every earlier block's points first, so that earlier block's own `<modeling>` clone
fails to resolve its source point and is silently dropped (`VPattern::ParseNodePoint()` catches
`VExceptionBadId` and returns) — leaving the piece's later reference to that clone to fail instead,
with an error like `ExceptionBadId: Can't find tool in table., id = N`. This check runs, and every
affected handler was restructured to run it, **before** any DOM element or modeling clone is
created, so a rejected action never leaves a dangling, unreferenced clone behind.

**Known gap in this check:** an operation-created destination point (`move`/`rotation`/
`mirrorByLine`/`mirrorByAxis`) has no individual entry in `doc->getHistory()` of its own — only
the *operation* tool's own id does — so such a point's own draft block cannot be determined this
way, and the check silently treats it as unverifiable rather than a violation. A cross-draft-block
reference through an operation's result specifically would not be caught. `piece.union` was
checked and confirmed **not** exposed to this failure class at all: it references only two
already-existing, already-self-contained pieces by name, never a raw calculation-scope point, so
there is no new clone for a draft-block mismatch to corrupt.

### `piece.list`

**Maps to:** no `Create()` call — reads `VContainer::DataPieces()` directly.
`src/libs/actionlayer/handlers/piece_list_handler.cpp:39`

Read-only (category `introspection`, grouped here by op-name prefix alongside the mutating
`piece.*` ops rather than under [Read-only introspection](#read-only-introspection) above).
Pieces are not `VGObject`s — `VContainer::DataPieces()` is a separate hash, keyed by id, from
`DataGObjects()` — so `pattern.dump` never lists them at all; this is how a caller discovers
which pieces exist (and their ids/names) before calling `piece.dump` or `render.snapshot`'s
`target: "piece"`.

Takes no parameters.

**Example request:**
```json
{ "op": "piece.list" }
```

**Example success response:**
```json
{ "pieces": [ { "id": 37, "name": "Square", "nodeCount": 4, "seamAllowance": true } ] }
```
Sorted by id (same `QHash`-iteration-order determinism fix `pattern.dump`'s `objects` array
needed — see [Findings](#findings)).

**Known error cases:** none — always succeeds (an empty/unloaded context just yields an empty array).

### `piece.dump`

**Maps to:** no `Create()` call — reads `VPiece::GetPath()`/`getInternalPaths()`/`getAnchors()`
and `VContainer::GetGObject()`/`getPiecePath()` directly.
`src/libs/actionlayer/handlers/piece_dump_handler.cpp:172`

Read-only (category `introspection`, same placement rationale as `piece.list` above).

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `piece` | string or number | yes | — | Name or numeric id of an existing piece (see `piece.list`). A JSON string is matched by name; a JSON number is taken as a literal piece id. |

**Example request:**
```json
{ "op": "piece.dump", "piece": "Square" }
```

**Example success response:**
```json
{
  "id": 37, "name": "Square", "op": "piece.dump",
  "seamAllowance": true, "seamAllowanceWidthFormula": "10",
  "nodes": [
    { "id": 12, "name": "__pieceNode_12", "type": "NodePoint", "reverse": false, "x": 0, "y": 0 }
  ],
  "internalPaths": [],
  "anchors": [ { "id": 15, "name": "A", "x": 0, "y": 0 } ]
}
```
`nodes` (the main outline path) and each `internalPaths[].nodes` entry report `{"id", "type",
"reverse", "name"?, "x"?, "y"?, "unsupported"?}` — `"type"` is the node's own `Tool` enumerator
name (`toolToString()`, shared with `pattern.dump`'s history entries rather than a second
stringification of the same enum); `"name"`/`"x"`/`"y"` only appear when the underlying object
resolves (and, for `"x"`/`"y"`, is itself a point) — note that a main-path/internal-path node's
`"name"` is the **clone's** own mangled `"__pieceNode_<id>"` name (see the module note above), not
the original draft point's name, since `piece.dump` reports `VGObject::name()` verbatim, the same
choice `pattern.dump` already makes for every object (a round-trip caller should compare
resolved `x`/`y` against the original point's own coordinates, not the node's `"name"`). `anchors`
entries have no `"type"`/`"reverse"` (anchors are plain point-clone ids, not wrapped in a
`VPieceNode`) and — unlike main-path/internal-path node clones — are **not** renamed, so an
anchor's `"name"` matches whatever the original anchored point was named.

`"unsupported": true` marks any node whose type this action layer's own `piece.addPatternPiece`/
`piece.internalPath` cannot construct today (anything other than `"NodePoint"` — see the arc/curve
node gap noted in the module comment above), or whose underlying object could not be resolved at
all (`VContainer::GetGObject()`/`getPiecePath()` throwing `VExceptionBadId`) — reading such a node
never crashes or silently drops it, it is always reported (`id`/`type`/`reverse` at minimum) with
this flag set instead. **Confirmed against a real, GUI-authored multi-piece file** (an Aldrich
block-style master pattern; see `tests/actionlayer/fixtures/patterns/`
`Aldrich-Womens-6th-Ed-Basic-Blocks.sm2d` and the `checkPieceDumpRealFile()` regression check in
`tests/actionlayer/run_batch/main.cpp`) that reading a real `NodeArc`/`NodeSpline` main-path node
neither crashes nor breaks the surrounding node list — only this action layer's own *creation*
path is limited to plain point nodes, not reading an existing piece built by the interactive GUI.

**Known error cases:** plain string for a missing `"piece"` argument or a missing data container;
structured `unknownPiece` (`{"type","message","piece"}`, same shape `piece.addAnchorPoint`/
`piece.internalPath`/`piece.insertNodes`/`piece.union` already use) for a `"piece"` that doesn't
resolve to any known piece.

### `piece.addPatternPiece`

**Maps to:** `PatternPieceTool::Create(...)` (id+`VPiece` overload) — `src/libs/vtools/tools/pattern_piece_tool.cpp:144`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `name` | string | yes | — | |
| `nodes` | array of strings | yes (≥3 entries) | — | Point names only (see the arc/curve-node gap above). Validated locally (`isSimpleClosedPolygon()`) to form a closed, non-self-intersecting polygon before `Create()` is ever called — `PatternPieceTool::Create()` has no such check of its own. |
| `seamAllowanceWidth` | string | yes | **formula** | |
| `seamAllowance` | bool | no (default `true`) | literal | Toggles `VAbstractPiece::hasSeamAllowance()` — a separate flag from the width formula itself, matching the interactive dialog's own "type a width" + "check the Seams checkbox" split. Since `seamAllowanceWidth` is required, supplying it always implies seam allowance should be enabled; `seamAllowance: false` lets a caller override that while still recording a width formula for later use. |
| `fill` | string | no (default `FillNone`) | — | A raw default-constructed `VPiece` fill value is not one of `VAbstractTool::fills()`'s recognized values and crashes `PatternPieceTool::RefreshGeometry()`'s `QBrush` construction (reproduced and root-caused) — this default exists specifically to avoid that, mirroring the interactive dialog's own always-seeded fallback. |
| `pieceColor` | string | no | — | Only set if given at all (no default applied). |
| `mx` / `my` | number | no | literal | Position offset (scene units) — `PatternPieceTool::RefreshGeometry()` applies `setPos(piece.GetMx(), piece.GetMy())` on top of the piece's own node-derived local shape (`pattern_piece_tool.cpp:1586`). **Bug fix, not a new feature:** `PatternPieceTool::Create()` never assigns a piece a position beyond `VPiece`'s default-constructed `(0,0)` — the same way the interactive GUI leaves a human to drag each new piece to a clear spot afterward — so every piece built with no explicit `mx`/`my` used to land at the exact same position, overlapping every other piece built the same way (reproduced directly: two pieces from two independently-drafted, coordinate-overlapping draft blocks rendered completely on top of each other). Giving **either** `mx` **or** `my` (even just one) is always taken exactly as given (the other defaulting to `0`) with **no auto-placement involved at all** — never adjusted. Omitting **both** auto-places the piece clear of every other piece this session has already assembled, via a session-lifetime `PieceLayoutCursor` (a simple non-overlapping left-to-right "shelf" layout — explicitly *not* a real cutting-layout/bin-packing optimization, out of scope the same way `export.scene`'s own "Phase B" note already flags that as separate, larger work). The response always echoes back the position actually used (`"mx"`/`"my"`), whichever path produced it. |
| `createGroup` | bool | no (default `true`) | literal | **Action-layer-specific usability addition, not a core-library bug fix.** `PatternPieceTool::Create()` itself never creates a group, even in the real, unmodified interactive GUI — a human there separately builds groups by hand via the Group Manager panel's own "+" button, an organizational step with no equivalent for a headless caller. Since this action layer has no human in the loop to perform that step, every multi-piece pattern built through it would otherwise leave the Group Manager permanently empty, unlike any comparable human-authored file — so this defaults **on**. When true, also creates a group (via `handleGroup()`, reused directly rather than reimplemented) containing this piece's own *original* node points (e.g. `"A"`/`"B"`/`"C"`/`"D"`) — not the piece's internal `"__pieceNode_<id>"`-named clones, which are never meant to be independently selected. A group-name collision (or any other `handleGroup()` failure) is reported via the response's `"groupError"` field rather than failing this whole action, since the piece itself has already been created successfully by that point. Set `false` to restore the old no-group behavior. |
| `groupName` | string | no | — | Name for the group `createGroup` creates; defaults to this piece's own `"name"` if omitted. Ignored when `createGroup` is false. |

**Example request:**
```json
{ "op": "piece.addPatternPiece", "name": "Square", "nodes": ["A", "B", "C", "D"],
  "seamAllowanceWidth": "10", "mx": 0, "my": 250 }
```

**Example success response:**
```json
{ "id": 37, "name": "Square", "mx": 0, "my": 250, "op": "piece.addPatternPiece",
  "group": { "id": 38, "name": "Square", "sourceObjects": ["A", "B", "C", "D"], "op": "group" } }
```
`"group"` is present whenever `createGroup` succeeded (the default, unless explicitly set
`false`); `"groupError"` (a structured or plain error, matching `handleGroup()`'s own shape) is
present instead if group creation failed. Neither field is present when `createGroup: false` was
given explicitly.

**Known error cases:** plain strings for missing `name`/`<3` nodes/missing `seamAllowanceWidth`,
or a resolved node that isn't a point; structured `nameResolution`; structured `invalidPiecePath`
(**new in this refresh — was missing from the error-type table**, `{"type","message","nodes"}`) if
the nodes don't form a simple closed polygon; structured `formulaError` for a bad
`seamAllowanceWidth`; structured `crossDraftBlockReference` (see [Pieces](#pieces)'s own note
above) if any node belongs to a different draft block than the currently active one — checked
before `nodes` gets cloned into any modeling clone, so a rejection never leaves one dangling. A
group-creation failure (whether `createGroup` was left at its default `true` or set explicitly)
does **not** fail this action — see `"groupError"` above.

### `piece.addAnchorPoint`

**Maps to:** `AnchorPointTool::Create(...)` — `src/libs/vtools/tools/nodeDetails/anchorpoint_tool.cpp:71`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `point` | string | yes | — | |
| `piece` | string | yes | — | Resolved through this file's own `resolvePieceId()` (pieces are **not** `VGObject`s — they live in `VContainer::DataPieces()`, a separate hash keyed by id, so `NameResolver` cannot resolve a piece name; this is the piece-specific equivalent, a plain linear scan for a matching `VPiece::GetName()`). |

No scene parameter needed — `AnchorPointTool::Create()`'s raw overload takes none.

**Example request:**
```json
{ "op": "piece.addAnchorPoint", "point": "A", "piece": "Square" }
```

**Example success response:**
```json
{ "id": 1, "point": "A", "piece": "Square", "op": "piece.addAnchorPoint" }
```
Note `"id"` here is the **point's own id**, not a new tool id — `AnchorPointTool` attaches an
existing point as a label anchor rather than creating anything new.

**Known error cases:** plain strings for missing `point`/`piece` or a non-point `point`; structured
`nameResolution`; structured `crossDraftBlockReference` (see [Pieces](#pieces)'s own note above) if
`point` belongs to a different draft block than the currently active one — checked before
`AnchorPointTool::Create()`'s own internal clone is created; structured `unknownPiece`
(`{"type","message","piece"}`) if `piece` doesn't resolve via `resolvePieceId()`.

### `piece.internalPath`

**Maps to:** `InternalPathTool::Create(...)` (id+`VPiecePath` overload) — `src/libs/vtools/tools/nodeDetails/internal_path_tool.cpp:78`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `piece` | string | yes | — | |
| `nodes` | array of strings | yes (≥2 entries) | — | Point names. Unlike `piece.addPatternPiece`, **not required to form a closed path** — no polygon-closure/self-intersection check applies. |
| `lineColor` / `lineWeight` | string | no | — | |
| `cutPath` | bool | no (default `false`) | literal | |

No `lineType` field, despite the header comment listing one — see [Findings](#findings).

**Example request:**
```json
{ "op": "piece.internalPath", "piece": "Square", "nodes": ["A", "C"] }
```

**Example success response:**
```json
{ "id": 38, "piece": "Square", "op": "piece.internalPath" }
```

**Known error cases:** plain strings for missing `piece`/`<2` nodes/a non-point node; structured
`nameResolution`; structured `crossDraftBlockReference` (see [Pieces](#pieces)'s own note above);
structured `unknownPiece`.

### `piece.insertNodes`

**Maps to:** `PatternPieceTool::insertNodes(...)` — `src/libs/vtools/tools/pattern_piece_tool.cpp:204`.
**Not** a `Create()` factory — a plain `static void` method with a reversed parameter order
`(nodes, pieceId, scene, data, doc)` compared to every other tool's `(..., scene, doc, data, ...)`
convention (verified against the declaration before writing this handler), so there is no new
object id to report back.

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `piece` | string | yes | — | |
| `nodes` | array of strings | yes (non-empty) | — | Point names, passed through as **original** point ids (not cloned/prepared) — see this section's intro for why this op alone doesn't need the clone step. |

**Example request:**
```json
{ "op": "piece.insertNodes", "piece": "Square", "nodes": ["E"] }
```

**Example success response:**
```json
{ "piece": "Square", "nodesInserted": 1, "op": "piece.insertNodes" }
```
No `"id"` field at all — matches the "no new object id" note above.

**Known error cases:** plain strings for missing `piece`/empty `nodes`/a non-point node; structured
`nameResolution`; structured `crossDraftBlockReference` (see [Pieces](#pieces)'s own note above);
structured `unknownPiece`.

**KNOWN GAP (unchanged since it was first documented):** unlike every `Create()`-based op in this
action layer, `insertNodes()` internally pushes a `SavePieceOptions` `QUndoCommand`
(`qApp->getUndoStack()->push(...)`) rather than mutating `data`/`doc` directly. One reproduction
surfaced a clean, catchable `VExceptionBadId` referencing a node id `PrepareNode()` had just
created moments earlier in the same call — not a crash (this handler's own `try`/`catch` reports
it as a normal structured failure, and the batch continues correctly), but its root cause inside
`SavePieceOptions`/`AddSANode`'s XML-serialization path was not resolved. **Verified against
current code during this refresh: the gap is still present, comment unchanged.**

### `piece.union`

**Maps to:** `UnionTool::Create(...)` — `src/libs/vtools/tools/union_tool.cpp:1521`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `piece1` / `piece2` | string | yes | — | Resolved via `resolvePieceId()`. |
| `piece1EdgeIndex` / `piece2EdgeIndex` | integer | yes | literal | 0-based, matching the interactive `UnionDialog`'s own edge-selection semantics. No auto-detection of a shared/co-located edge — `UnionTool::Create()` itself takes an explicit index pair, not a search. |
| `retainPieces` | bool | no (default `false`) | literal | |

**Example request:**
```json
{ "op": "piece.union", "piece1": "Front", "piece2": "Back", "piece1EdgeIndex": 0, "piece2EdgeIndex": 0 }
```

**Example success response (documented for completeness — do not rely on ever seeing this in
practice, see below):**
```json
{ "id": 39, "piece1": "Front", "piece2": "Back", "op": "piece.union" }
```

**Known error cases:** plain strings for missing fields; structured `nameResolution`; structured
`unknownPiece` for either piece name. **Not** subject to the `crossDraftBlockReference` check
(see [Pieces](#pieces)'s own note) — confirmed this op never references a raw calculation-scope
point at all, only two already-existing, already-self-contained pieces by name.

**KNOWN GAP — DO NOT RELY ON THIS OP YET (unchanged since it was first documented):**
reproducibly **segfaults** inside `UnionTool::Create()` → `unitePieces()` →
`UnionInitParameters()`/`createUnion()` (`union_tool.cpp`) against two plain rectangular pieces,
with both a plausible shared-edge index pair and a deliberately-mismatched one (`0`/`0`) — same
crash either way, so it is not simply a bad-index input this handler's own validation could catch.
The one op in this entire action layer whose raw-overload code path was not exercisable to a clean
success or a clean, catchable error. **Verified against current code during this refresh: the gap
is still present, comment unchanged** (`piece_handlers.h`'s own `KNOWN GAP` comment, and
`tests/actionlayer/scripts/06_piece_and_union.json`'s own `_note` field, both still exclude this
op from any automated run for exactly this reason).

## Measurements

Distinct from `pattern.listMeasurements` (read-only). All mirror
`MainWindow::openMeasurementFile()`/`updateMeasurements()`/`syncMeasurements()`
(`src/app/seamly2d/mainwindow.cpp`) line-for-line, without any `MainWindow` dependency.

### `measurements.load`

**Maps to:** no `VTool::Create()` — `MeasurementDoc` (`src/libs/vformat/measurements.h`) parsing +
`VContainer::ClearVariables()`/the measurement doc's own `readMeasurements()`.
`src/libs/actionlayer/handlers/measurements_sync_handlers.cpp:362`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `path` | string | yes | — | Measurement file (`.smis`/`.smms`/`.vst`). |
| `size` / `height` | integer | required **only** for a multisize file | literal | Applied verbatim, no unit conversion (unlike the *initial* pattern load, which does convert) — mirrors `syncMeasurements()`'s own re-pass of `VContainer::size()`/`height()`. Ignored for an individual-measurement file, which instead derives size/height from its own `size_M`/`height_M` special variables. |

Does **not** recompute pattern geometry — call `measurements.recompute` (or use
`measurements.sync`) for that.

**Example request:**
```json
{ "op": "measurements.load", "path": "sizes.smis" }
```

**Example success response:**
```json
{ "success": true, "measurementsLoaded": 9, "type": "individual" }
```

**Known error cases:** plain strings for missing/nonexistent `path`, an unreadable/unrecognized
file, a schema-upgrade failure, an invalid known measurement name, multisize-in-inches, a
`readMeasurements()` parse failure, or a missing `size`/`height` for a multisize file; structured
`missingMeasurements` (`{"type","message","missing"}`) if the file omits a measurement the pattern
requires; structured `measurementTypeMismatch` (`{"type","message","expected","actual"}`) if the
file's type (individual/multisize) doesn't match what the pattern was already established with.
**Check-then-mutate ordering, verified:** every validation check above runs strictly before any
state mutation — a failed check leaves `VContainer`/`doc` byte-identical to before the action ran.

### `measurements.recompute`

**Maps to:** no `VTool::Create()` — `VAbstractPattern::LiteParseTree(Document::LiteParse)`.
`src/libs/actionlayer/handlers/measurements_sync_handlers.cpp:378`/`294`

Takes no parameters. Generically useful standalone, not just after a measurement change — it is
simply "re-evaluate every formula in the pattern against `VContainer`'s current variables and
rebuild geometry."

**Example request:**
```json
{ "op": "measurements.recompute" }
```

**Example success response:**
```json
{ "success": true }
```

**Known error cases:** structured `formulaError` — the **only** failure mode this handler can
actually detect (verified by reading `VPattern::LiteParseTree()`'s body directly): it already
catches every `VException` subtype plus `std::bad_alloc` internally and, on failure, calls
`qApp->exit(...)`, which is a silent no-op in `actiond` (no running event loop to stop) — so a
`VException`-family recompute failure is swallowed by `LiteParseTree()` itself and reported here as
success, not as a detected error. `qmu::QmuParserError` is the one exception type
`LiteParseTree()` does **not** catch (it isn't a `VException` subclass), so it is the one failure
mode this handler can observe and report — also the realistic dominant real-world case for a
measurement-triggered recompute specifically (a formula newly dividing by a now-zero measurement).

### `measurements.sync`

**Maps to:** composes the exact same private helpers `measurements.load`/`measurements.recompute`
call — not a second implementation, not a re-dispatch through `ActionRegistry`.
`src/libs/actionlayer/handlers/measurements_sync_handlers.cpp:385`

Same parameters as `measurements.load`.

**Example request:**
```json
{ "op": "measurements.sync", "path": "sizes.smis" }
```

**Example success response:**
```json
{ "success": true, "measurementsLoaded": 9, "type": "individual", "recomputed": true }
```

**Known error cases:** every `measurements.load` error case (if the load half fails, nothing is
mutated and recompute never runs); if load succeeds but the recompute half then fails, the error
is reported with `measurementsLoaded`/`type` attached (state **was** already mutated by the
successful load half by that point — this is called out explicitly so a caller doesn't assume
"sync failed" means "nothing changed").

## Session lifecycle

### `session.save`

**Maps to:** no `VTool::Create()` — `VDomDocument::SaveDocument()` (reached through the
`VAbstractPattern*` pointer). `src/libs/actionlayer/handlers/session_handlers.cpp:36`

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `path` | string | yes | — | Resolved relative to the process's current working directory (the daemon sets this to `--output-dir` at startup). Parent directory created if missing. |

**Example request:**
```json
{ "op": "session.save", "path": "square.val" }
```

**Example success response:**
```json
{ "path": "C:/.../square.val" }
```
`"path"` is the resolved **absolute** path actually written to, not the (possibly relative) input.

**Known error cases:** plain strings for missing `path`, a directory-creation failure, or a
`SaveDocument()` failure (reported via its own out-parameter, not an exception).

### `session.close`

**Maps to:** no `VTool::Create()` — a pure no-op handler; the real effect is external, in
`SessionServer`'s own read loop, which exits after this action's batch response is written.
`src/libs/actionlayer/handlers/session_handlers.cpp:69`

Takes no parameters. Registered as a normal action (rather than only special-cased in the protocol
loop) so it participates in ordinary results/`appliedCount` bookkeeping and shows up in
`pattern.listTools` like every other op.

**Example request:**
```json
{ "op": "session.close" }
```

**Example success response:**
```json
{ "closing": true }
```

**Known error cases:** none — always succeeds.

## Undo

### `pattern.undo`

**Maps to:** no single `VTool::Create()` — reverses `doc->getHistory()`'s tail, most-recently-
created entry first, via `DelTool`/`DeletePiece`/`DeleteDraftBlock` (matched per entry's `Tool`
type), called directly (not pushed onto any `QUndoStack`).
`src/libs/actionlayer/handlers/history_undo_handlers.cpp:118`

**Design note — read this before using this op:** this replaced an earlier, abandoned
`QUndoStack`-based `session.undo`/`session.redo`/`session.undoStatus` design. That design's
`QUndoCommand` objects hold live pointers into one process's in-memory `VPattern`/`VContainer`, so
they cannot survive `session.save` → process exit → a new process reloading the file — which
defeats the point for an AI caller working across separate `actiond` invocations against the same
saved pattern. `pattern.undo` instead works at the DOM/history level: its effect is exactly what a
subsequent `session.save` writes, with no live-vs-saved distinction to get wrong. The trade-off is
explicit and permanent: **`pattern.undo` is one-directional — there is no `pattern.redo`.**
Verified directly (three separate `actiond` processes: build `A`+`B`, save, exit; reload, build
more, save, exit; reload fresh, `pattern.undo` back past the first save's boundary) — the reverted
pattern's `pattern.dump` and re-saved `.val` file were confirmed byte-identical to the first save.

| Parameter | Type | Required | Literal / Formula | Description |
|---|---|---|---|---|
| `count` | number | no (default 1) | Literal | How many history entries to undo, most-recently-created first. 0 is a valid no-op; negative is a hard error. Stops early, without erroring, once history is exhausted. |

**Example request:**
```json
{ "op": "pattern.undo", "count": 2 }
```

**Example success response:**
```json
{ "undone": 2, "remaining": 1, "entries": [ { "id": 3, "kind": "tool" }, { "id": 2, "kind": "tool" } ] }
```
`"entries"` lists what was actually removed, most-recent first; `"kind"` is `"tool"` (the common
case — points, lines, curves, cut-points, operations, and modeling-scope entries like
`InternalPath`/`AnchorPoint`/`NodePoint`), `"piece"` (a `piece.addPatternPiece` entry, reversed via
`DeletePiece`), or `"draftBlock"` (a `basePoint` entry, reversed via `DeleteDraftBlock` — this tears
down the *whole* draft block that `basePoint` action created, not just its one point).

**Safety property this relies on:** Seamly2D's pattern format only lets a formula reference an
object that already existed when the referencing tool was created, so undoing strictly
most-recently-created-first can never hit an object something else still depends on. This op always
walks the history tail in that order and never reorders it — do not assume a "delete this specific
id, skip dependents" mode exists; it doesn't, by design.

**KNOWN GAP:** `group` (`operation_handlers.cpp`) and `piece.insertNodes` (`piece_handlers.cpp`)
build their DOM directly without ever calling `VAbstractTool::AddRecord()`, so neither has any entry
in `doc->getHistory()` at all — `pattern.undo` has no way to reach either op's effect; `"count"`
silently counts past it onto an older entry instead. This is a pre-existing gap in what
`doc->getHistory()` itself tracks, not something `pattern.undo` introduces.

**Known error cases:** a plain string for a negative `"count"`; a structured
`{"type":"undoInconsistency",...}` object (should not occur in normal use — see this op's own
implementation comment) if a history entry's id is already gone from live pattern state when this
op tries to reverse it.

## Coverage summary

Cross-references every op documented above against Seamly2D's own `Tool` enum
(`src/libs/vmisc/def.h`, as stringified by `pattern_dump_handler.cpp`'s `toolToString()`) — every
tool type Seamly2D's core knows about, not just what the action layer happens to implement.
Abstract/base enum values (never a standalone creatable tool) are marked N/A rather than "not
implemented."

| Seamly2D `Tool` enumerator | Action-layer op(s) | Status |
|---|---|---|
| `BasePoint` | `basePoint` | Implemented |
| `Line` | `line` | Implemented |
| `EndLine` | `endLine` | Implemented |
| `AlongLine` | `alongLine` | Implemented |
| `Normal` | `normal` | Implemented |
| `Bisector` | `bisector` | Implemented |
| `ShoulderPoint` | `shoulderPoint` | Implemented |
| `LineIntersect` | `lineIntersect` | Implemented |
| `Spline` | `spline` | Implemented |
| `SplinePath` | `splinePath` | Implemented |
| `CubicBezier` | `cubicBezier` | Implemented |
| `CubicBezierPath` | `cubicBezierPath` | Implemented |
| `Arc` | `arc` | Implemented |
| `ArcWithLength` | `arcWithLength` | Implemented |
| `EllipticalArc` | `ellipticalArc` | Implemented |
| `CutSpline` | `cutSpline` | Implemented |
| `CutArc` | `cutArc` | Implemented |
| `PointOfIntersectionArcs` | `pointOfIntersectionArcs` | Implemented |
| `PointOfIntersectionCircles` | `pointOfIntersectionCircles` (via `IntersectCirclesTool`) | Implemented |
| `PointOfIntersectionCurves` | `pointOfIntersectionCurves` | Implemented |
| `CurveIntersectAxis` | `curveIntersectAxis` | Implemented |
| `ArcIntersectAxis` | `curveIntersectAxis` (covers arcs too) | N/A — reserved enum value, no live tool class (confirmed in `pattern_dump_handler.cpp`) |
| `PointFromCircleAndTangent` | `pointFromCircleAndTangent` (via `IntersectCircleTangentTool`) | Implemented |
| `PointFromArcAndTangent` | `pointFromArcAndTangent` | Implemented |
| `Triangle` | `triangle` | Implemented |
| `Height` | `height` | Implemented |
| `Move` | `move` | Implemented |
| `Rotation` | `rotation` | Implemented |
| `MirrorByLine` | `mirrorByLine` | Implemented |
| `MirrorByAxis` | `mirrorByAxis` | Implemented |
| `Group` | `group` | Implemented |
| `TrueDarts` | `trueDarts` | Implemented |
| `Union` | `piece.union` | **Partial** — implemented but reproducibly segfaults; do not use |
| `Piece` | `piece.addPatternPiece` | Implemented |
| `AnchorPoint` | `piece.addAnchorPoint` | Implemented |
| `InternalPath` | `piece.internalPath` | Implemented |
| `InsertNodes` | `piece.insertNodes` | **Partial** — implemented, one reproduced (caught, non-fatal) exception path not root-caused |
| `NodePoint` | (used internally by piece-node construction) | Implemented, internal-only — not a standalone op |
| `NodeArc` / `NodeElArc` / `NodeSpline` / `NodeSplinePath` | — | **Not yet implemented** — piece `"nodes"` arrays only accept point names today (documented gap in `piece_handlers.h`) |
| `Midpoint` | (covered by `alongLine` with `length: "..._length/2"`-style formulas) | N/A — shares `AlongLine`'s tool type in practice, per `toolToString()`'s own comment; no separate op needed |
| `CutSplinePath` | — | **Not yet implemented** — confirmed a real, distinct live tool class (`VToolCutSplinePath`, `src/libs/vtools/tools/drawTools/toolpoint/toolsinglepoint/toolcut/vtoolcutsplinepath.h`), not merely a reserved enum value; `cutSpline`'s own handler only constructs `VToolCutSpline`, never this class, so a splinePath cut via this dedicated tool (as opposed to `cutSpline` accepting a splinePath as its `"curve"` input, which is a different code path) has no action-layer op |
| `PointOfContact` | — | **Not yet implemented** |
| `LineIntersectAxis` | — | **Not yet implemented** — confirmed a real, distinct live tool class (`VToolLineIntersectAxis`, `.../toollinepoint/vtoollineintersectaxis.h`, with its own dialog `dialoglineintersectaxis.h`/`.ui`), separate from `curveIntersectAxis` (`VToolCurveIntersectAxis`) — this refresh only traced `curveIntersectAxis`'s own handler, which never references `VToolLineIntersectAxis` |
| `PointOfIntersection` | — | **Not yet implemented / unverified** — no `vtoolpointofintersection.h` file exists under `src/libs/vtools`, suggesting this enumerator (like `ArcIntersectAxis`) may be reserved/superseded by `LineIntersect` rather than a real gap, but this was not confirmed as conclusively as `ArcIntersectAxis` (which has an explicit in-code comment saying so) — flagged rather than asserted |
| `BackgroundImage` | — | **Not yet implemented** |
| `Arrow` / `SinglePoint` / `DoublePoint` / `LinePoint` / `AbstractSpline` / `Cut` | — | N/A — abstract/base enum categories, never a standalone creatable tool |
| `LAST_ONE_DO_NOT_USE` | — | N/A — sentinel value |

Non-`VTool::Create()`-based introspection/session ops with no `Tool` enum counterpart at all:
`pattern.dump`, `pattern.listMeasurements`, `pattern.listTools`, `pattern.resolveName`,
`render.snapshot`, `export.scene`, `point.edit`, `measurements.load`, `measurements.recompute`,
`measurements.sync`, `session.save`, `session.close`, `pattern.undo`, `piece.list`, `piece.dump`.

**51 ops registered in `action_registry.cpp` total** (48 as of the 21 Aug 2026 export-effort
refresh, plus `pattern.undo` added 22 Aug 2026 — Phase 12, undo, second design; see this doc's own
`pattern.undo` entry above for why a first, `QUndoStack`-based design was built, then abandoned and
replaced; plus `piece.list`/`piece.dump` added 22 Aug 2026 alongside `render.snapshot`'s `target:
"piece"` extension — see [CHANGELOG.md](../CHANGELOG.md)): 48 fully implemented and exercised
successfully via a real `actiond` run at some point during development, 2 partial (`piece.union`,
`piece.insertNodes`) with documented, reproduced gaps, and 1 partial (`export.scene`, only in the
sense that its `ps`/`eps` formats depend on an external `pdftops` binary — every other format has
no such dependency).

## Findings

Issues surfaced by reading every handler's actual source during this refresh, rather than
transcribing the previous draft of this file. None of these are fixed here — this is a
documentation pass, not a bugfix pass, per the task that produced this refresh.

- **No JSON-field-to-`Create()`-parameter mismatches were found.** Every one of the 47 handlers
  was read in full and its `Create()` call site traced argument-by-argument against the field it
  reads. Every mapping is exactly what its own field name suggests (e.g. `endLine`'s JSON
  `"angle"` really does reach `Create()`'s angle-formula parameter, not its length parameter).
  This is worth stating explicitly rather than leaving as an absence, since it was the primary
  thing this refresh was asked to check for.
- **Four structured error `"type"` values existed in the code but were missing from this file's
  error-type table**: `toolNotFound` (`point.edit`), `invalidPiecePath`
  (`piece.addPatternPiece`), `unknownPiece` (`piece.addAnchorPoint`/`piece.internalPath`/
  `piece.insertNodes`/`piece.union`), `groupExists` (`group`). Added to the table above.
- **`render.snapshot` had five real, implemented parameters absent from the previous version of
  this file's example**: `target`, `format`, `background`, `padding`, `highlight`. Only `path`/
  `width`/`height` were previously shown. All five are now documented above.
- **`group`'s actual success-response shape was never shown anywhere in this file before** — the
  Operations section's general statement ("every op below except `group` returns
  `{"created":[...],"op"}`") only said what `group` does *not* return, never what it *does*.
  Documented above: `{"id","name","sourceObjects","op"}`.
- **`point.edit`'s two `formulaError` objects are shaped differently from every other handler's
  `formulaError`.** Every `Create()`-based handler's shared `runCreate()` helper builds
  `{"type","message","op","expr"}` from a caught `qmu::QmuParserError`. `point.edit` instead
  validates via a standalone `VFormula`/`.Eval()`/`.error()` check (there is no `Create()` call to
  wrap) and builds a shorter `{"type","message"}`-only object with no `op`/`expr` fields on
  failure. Not a bug — `point.edit` never actually catches a `qmu::QmuParserError` at all, it
  checks `VFormula::error()` directly — but a caller branching on `formulaError`'s `"expr"` field
  specifically would need to handle this handler's `formulaError` not having one.
- **`piece.internalPath`'s header comment lists a `"lineType"` parameter that the implementation
  never reads.** `piece_handlers.h`'s doc comment for `piece.internalPath` says
  `{"piece","nodes":["..."],"lineColor"?,"lineType"?,"lineWeight"?,"cutPath"?}`, but
  `handlePieceInternalPath()`'s body only calls `lineColorOrDefault(args)`/
  `lineWeightOrDefault(args)`/`args.value("cutPath")` — no `lineTypeOrDefault(args)` call anywhere,
  and `VPiecePath` has no `SetPenStyle()`/line-type setter called at all in this function (contrast
  with `piece.addPatternPiece`, whose own path likewise never sets one either, and with
  `piece.union`, `piece.insertNodes`, `piece.addAnchorPoint`, none of which take a `lineType` at
  all). This is a genuine doc/code mismatch in the **pre-existing header comment** (not something
  this refresh introduced) — a caller supplying `"lineType"` to `piece.internalPath` today has it
  silently ignored. This file's own `piece.internalPath` entry above already omits `lineType` from
  its parameter table to reflect actual behavior; flagged here as a candidate one-line follow-up
  fix to `piece_handlers.h`'s comment (out of scope for this documentation-only pass).
- **`lineType` values are not schema-validated at write time** (carried over from the previous
  version of this file — still true, re-verified against `line_handlers.cpp`/
  `formula_point_handlers.cpp`/`curve_handlers.cpp`/`cutpoint_handlers.cpp`, none of which validate
  their `lineType` string against the schema's actual enumeration: `solidLine`, `dashLine`,
  `dotLine`, `dashDotLine`, `dashDotDotLine`, `none`, `byGroup` — see
  `src/libs/ifc/schema/pattern/v0.7.4.xsd`). An invalid value writes successfully and only fails
  the *next* time that file is loaded. `tests/actionlayer/scripts/*.json` avoid this entirely by
  omitting `lineType` (falling back to each handler's own `solidLine` default).
- **Two `Tool` enum values are confirmed real, live tool classes with no action-layer op at all**:
  `CutSplinePath` (`VToolCutSplinePath`) and `LineIntersectAxis` (`VToolLineIntersectAxis`, with
  its own dialog) — verified by finding their header files directly, not inferred from the enum
  alone. Genuine coverage gaps for a future phase, not documented-but-broken like `piece.union`/
  `piece.insertNodes`. A third, `PointOfIntersection`, has no matching header file under
  `src/libs/vtools` at all (unlike the two above), suggesting it may be reserved/dead the same way
  `ArcIntersectAxis` explicitly is — but that was not confirmed with the same certainty (no
  in-code comment says so the way `ArcIntersectAxis`'s does), so it is flagged as unverified rather
  than asserted dead. See the coverage table above.
